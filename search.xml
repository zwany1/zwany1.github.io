<?xml version="1.0" encoding="UTF-8"?>
<search>
<entry>
<title>Redis 主从复制</title>
<content>首先我先说说主从复制是什么？为了减轻redis的压力，使用了读写分离的形式，也就是主节点写，从节点读，这样就要求主从节点数据必须一致，所以就有了主从复制。主从复制就是，将一个主节点上的数据全部复制到其他的从节点上，以保持主从节点的数据一致。具体怎么实现的呢？首先，其他节点需要先向主节点发送psync命令来发起开始同步。接着，主节点通过发送来的psync来判断是进行全局同步还是增量复制。假如是第一次进行同步，或者是之前的连接失效，就会进行全量复制。全量复制完毕后，主从节点之间会维护一个长链接，在此期间会将主节点的操作进行增量复制到从节点，维护主从节点一致性。为什么需要主从复制？  降低数据冗余: 主从复制实现了数据的热备份，是持久化之外的一种数据冗余方式。  提供故障恢复： 当主节点出现问题时，可以由从节点提供服务，实现快速的故障恢复；  支持负载均衡: 主从复制配合读写分离，主节点写服务，从节点读服务，写数据连主节点，读数据连从节点，可分担服务器负载。写少读多场景下，多个从节点分担读负载，提高 Redis 服务器并发量。  高可用： 主从复制是 Redis 高可用的基础，也是哨兵和集群实施的基础。主从库之间采用的是 读写分离 的方式：  读操作: 主库、从库可以接收  写操作: 仅主库可执行，然后主库将写操作同步给从库两种同步复制方式  全量复制： 第一次同步时  增量复制: 只会把主从库网络断联期间主库收到的命令同步交给从库全量复制基于 TCP 长连接避免频繁建立 TCP 链接和断开带来的性能开销，分为三阶段如图：  假设主机 Master 在 6379 端口，从机 Slave 在 6380端口; 基于本机 (方便理解后续扩展内容: 搭建一主多从)  其中 repl buffer 是 replication buffer 简写第一阶段slave 向 master 发送 psync指令，表示进行数据同步，psync包含两个参数，分别是 主服务器的 runID 和 复制进度 offset  runID: 标识每一个 Redis 服务器  offset: 第一次同步表示 -1FULLRESYNC 响应命令意图是全量复制，主服务器将所有数据同步给从服务器。第二阶段Master 执行 bgsave 指令来生成 RDB 文件，然后把文件发送给从服务器Slave 接收到 Master 传输的 RDB 之后，会先清空当前的数据，然后载入 RDB 文件Master 通过 bgsave指令会产生一个子进程来进行 RDB 文件生成的工作，是异步工作的。  这期间写操作命令未记录到刚生成的 RDB 文件中，主从服务器数据不一致。为了保证一致性，Master 在三个时间间隙将写操作命令写入 replication buffer 缓冲区。  Master 生成 RDB 文件期间  Master 发送 RDB 文件给 Slave 服务器期间  Slave 加载 RDB 文件期间第三阶段Master 生成的 RDB 文件发送完，Slave 收到后，丢弃旧数据，载入 RDB 数据到内存，完成载入后回复确认消息给 Master。然后 Master 需要将这期间记载的在 replication buffer 缓冲区内的写操作记录也发送给 Slave, 保证一致性问题：但是第一次全量复制之后如果遇到网络问题无法进行命令传播，Slave 就无法和 Master 保持同步了，导致不一致性 而如果恢复连接之后使用全量复制保证一致性的话开销太大，因为有一部分数据已经一致。有什么办法吗？增量复制图示：  repl backlog 表示 repl_backlog_buffer  repl buffer 表示 replication buffer过程  服务器恢复网络后，会向主服务器发送 psync 命令（offset 非 -1）。  主服务器收到后，用 CONTINUE 响应命令告知从服务器采用增量复制同步数据。  接着，主服务器将断线期间执行的写命令发给从服务器，从服务器执行这些命令。其中:  repl_backlog_buffer 是一个 ⌈环形⌋ 缓冲区，用于主从服务器断开连接后，从中找到差异的数据  replication offset标记同步进度，Master 用于记录自己写的未知，Slave 记录自己读的未知repl_backlog_buffer 缓存的写入时机  Master 在将写指令发送给 Slave 的同时，将其写入到 repl_backlog_buffer 缓冲区内如何辨别应该向 Slave 执行全量复制还是增量复制Slave 通过 psync指令将自己的 slave_repl_offset 发送给主服务器；服务器依据 master_repl_offset 与 slave_repl_offset 的差距决定对从服务器的同步操作。  若从服务器所需数据在 repl_backlog_buffer 缓冲区，主服务器采用增量同步；  若不在，主服务器采用全量同步。由于 repl_backlog_buffer 缓冲区的默认大小是 1M。 由于时一个环形缓冲区，如果写满，会发生 覆盖问题 。网络恢复时，若服务器想读的数据被覆盖，主服务器采用全量同步，此方式比增量同步性能损耗大很多。因此为了避免频繁的全量同步，应该调大 repl_backlog_buffer推荐大小second * write_size_per_second  second 为从服务器断线后重新连接上主服务器所需的平均时间 (单位 s)  write_size_per_second 则是主服务器平均每秒产生的写命令数据量大小。支持基于 redis.conf 配置项修改 repl_backlog_buffer 缓冲区大小(扩展) - 如何搭建一主多从？创建一个测试目录修改 redis.conf  确认其中的配置项为 daemonize = yes并且仅开启 RDB 备份持久化策略  关闭混合持久化策略 aof-user-rdb-preamble 设置为 no  关闭 AOF 持久化策略 appendonly 设置为 no 分别创建三个配置文件示例: redis6379.conf、redis6380.conf、redis6381.conf并且分别添加如下配置include [放置配置文件的目录路径]/redis.confpidfile /var/run/redis_6379.pidport [基于文件后缀，比如 6379 端口]dbfilename dump[端口].rdb参考配置分别启动三台 redis 服务器连接查看相关信息  指令 info replication 用于打印主从复制的相关信息配置主从关系将 6380 和 6381 配置成为 Slave ; 而 6379 作为主机 Master 借助如下指令，配置某个实例的从服务器  指令 slaveof &amp;lt;master_ip&amp;gt; &amp;lt;master_port&amp;gt;127.0.0.1:6380&amp;gt; slaveof 127.0.0.1 6379OK127.0.0.1:6380&amp;gt; info replication# Replicationrole:slavemaster_host:127.0.0.1master_port:6379master_link_status:upmaster_last_io_seconds_ago:4master_sync_in_progress:0slave_read_repl_offset:0slave_repl_offset:0slave_priority:100slave_read_only:1replica_announced:1connected_slaves:0master_failover_state:no-failovermaster_replid:6635dfe974961f6624785748ea2ed035bc670b99master_replid2:0000000000000000000000000000000000000000master_repl_offset:0second_repl_offset:-1repl_backlog_active:1repl_backlog_size:1048576repl_backlog_first_byte_offset:1repl_backlog_histlen:0 查看主机信息配置了两个从节点  一个为端口 6380 的服务  一个为端口 6381 的服务测试: 主机支持读写，但是从机仅支持读主机 Master 支持读写 从机 Slave 仅支持读  尝试写报错: READONLY You can’t write against a read only replice.  (扩展) - 测试主从复制 之 全量复制流程简述：  Slave 启动成功会连接到 master 后会发送一个 sync 指令  Master 接到命令启动后台的存盘进程，同时收集所有接收到的用于修改数据的指令，在后台进程执行完毕之后，Master 将传送整个数据文件到 slave, 完成一次完全同步  Slave 服务在接收到数据库文件数据之后，将其存盘并且加载到内存之中，即 全量复制测试：Slave down 后重启仍然获取到 Master 最新数据Master 添加数据  推荐这个好用的 Redis 可视化工具 Another Redis Desktop Manager (๑•̀ㅂ•́)و✧查看从库  已经自动同步停止从库主库继续添加新的数据当前还存活: 6379 主(Master)， 6380 从（Slave） 重启从库 6381 从库启动之后，默认仍然是 master 再次将其绑定 Master, 设置为从库 获取从库内的所有 key  进行全量复制，6381从库仍然可以得到最新的数据主库 Master 内此时的最新数据 测试：Master 意外 down 后重启自动恢复 Master 地位现象: 当 master 恢复之后，从服务器 Slave 仍然指向原来的主服务器在 6379 主 Master 服务器内执行SHUTDOWN确认 Master 已经 Down 此时从服务器 重启端口为 6379 的 Master查看当前各个服务节点是否启动[root@xxx]# redis-server redis6379.conf[root@xxx]# ps -ef | grep redisroot      372752       1  0 06:30 ?        00:00:10 redis-server *:6380root      374411       1  0 06:50 ?        00:00:03 redis-server *:6381root      374416  366219  0 06:50 pts/1    00:00:00 redis-cli -p 6381root      375125       1  5 06:58 ?        00:00:00 redis-server *:6379root      375135  374917  0 06:58 pts/2    00:00:00 grep --color=auto redis6379主服务器启动之后仍然是 master查询从服务器 Slave 信息 可以监控到主服务器已经启动 提问：从节点能不能再挂从节点？形成级联复制？回答：可以的。从节点可以配置成另一个从节点的主节点，形成链式结构。这样做的好处是减轻主节点的复制压力。假设有 10 个从节点都直接连主节点，主节点要维护 10 个 replication buffer，要发 10 份数据。如果用级联结构，主节点只连 2 个从节点，这 2 ...</content>
<url>https://zwany1.github.io/2026/08/28/redis/</url>
</entry>
<entry>
<title>IO基础与流操作</title>
<content>IO流核心概念Java IO包含面向字节与面向字符两类抽象。字节流处理原始二进制数据，字符流在字节与字符之间进行编码转换。阻塞IO在读写时占用线程，NIO通过缓冲区与通道支持更高效的文件与网络传输。IO（Input/Output）是指数据的输入与输出操作。当应用程序需要从外部存储（如文件、数据库、网络）读取数据到内存时,这个过程称为输入;反之,将内存中的数据写出到外部存储的过程称为输出。由于数据传输的特性类似于水流,因此我们将这种数据传输机制称为IO流。Java的IO流体系以4个抽象基类为核心,派生出40多个具体实现类:  InputStream / Reader: 输入流的抽象基类,前者处理字节数据,后者处理字符数据  OutputStream / Writer: 输出流的抽象基类,前者处理字节数据,后者处理字符数据字节流操作InputStream核心方法InputStream是所有字节输入流的抽象父类,用于从数据源读取字节数据到内存。核心API方法:read(): 读取单个字节,返回0-255之间的int值,返回-1表示流结束read(byte[] b): 将数据读入字节数组,返回实际读取的字节数read(byte[] b, int off, int len): 指定偏移量和读取长度的读取操作skip(long n): 跳过n个字节available(): 返回可读取的字节数估计值close(): 关闭流并释放系统资源JDK 9新增的便捷方法:readAllBytes(): 一次性读取所有字节readNBytes(byte[] b, int off, int len): 阻塞直到读取len个字节transferTo(OutputStream out): 将所有字节传输到输出流FileInputStream文件读取示例​FileInputStream是最常用的字节输入流实现,可直接读取文件内容:public class LogFileReader {    public void readAccessLog(String logPath) {        try (InputStream fis = new FileInputStream(logPath)) {            System.out.println(&quot;日志文件可读字节数: &quot; + fis.available());                        // 跳过前10个字节(通常是时间戳)            long skipped = fis.skip(10);            System.out.println(&quot;已跳过字节数: &quot; + skipped);                        System.out.print(&quot;访问记录内容: &quot;);            int byteData;            while ((byteData = fis.read()) != -1) {                System.out.print((char) byteData);            }        } catch (IOException e) {            e.printStackTrace();        }    }}假设access.log文件内容为:2024-11-26 UserLoginSuccess输出结果:日志文件可读字节数: 33已跳过字节数: 10访问记录内容:  UserLoginSuccessBufferedInputStream缓冲读取​实际项目中,我们通常会搭配BufferedInputStream来提升读取性能:public class ConfigLoader {    public String loadConfig(String configPath) throws IOException {        try (BufferedInputStream bis = new BufferedInputStream(                new FileInputStream(configPath))) {            // 使用JDK 9+的便捷方法一次性读取全部内容            return new String(bis.readAllBytes());        }    }}DataInputStream类型化读取​DataInputStream可以读取Java基本数据类型,常用于读取二进制配置文件:public class BinaryConfigReader {    public void readDatabaseConfig(String path) throws IOException {        try (DataInputStream dis = new DataInputStream(                new FileInputStream(path))) {            boolean isEnabled = dis.readBoolean();      // 读取布尔值            int maxConnections = dis.readInt();         // 读取整数            String dbUrl = dis.readUTF();               // 读取字符串                        System.out.println(&quot;数据库启用状态: &quot; + isEnabled);            System.out.println(&quot;最大连接数: &quot; + maxConnections);            System.out.println(&quot;数据库URL: &quot; + dbUrl);        }    }}ObjectInputStream对象反序列化​ObjectInputStream用于反序列化Java对象,对象必须实现Serializable接口:public class UserSessionManager {    public UserSession restoreSession(String sessionFile) throws Exception {        try (ObjectInputStream ois = new ObjectInputStream(                new FileInputStream(sessionFile))) {            return (UserSession) ois.readObject();        }    }}class UserSession implements Serializable {    private static final long serialVersionUID = 1L;        private String userId;    private String sessionToken;    private transient String password; // transient字段不会被序列化        // getters and setters...}OutputStream核心方法​OutputStream是所有字节输出流的抽象父类,用于将字节数据写入目标位置。核心API方法:write(int b): 写入单个字节write(byte[] b): 写入字节数组write(byte[] b, int off, int len): 写入字节数组的指定部分flush(): 刷新缓冲区,强制写出缓存数据close(): 关闭流并释放资源FileOutputStream文件写入示例​public class AuditLogger {    public void logUserAction(String action) {        try (FileOutputStream fos = new FileOutputStream(&quot;audit.log&quot;, true)) { // true表示追加模式            byte[] actionBytes = action.getBytes();            fos.write(actionBytes);            fos.write(&#39;\n&#39;); // 换行        } catch (IOException e) {            e.printStackTrace();        }    }}实际项目中通常搭配BufferedOutputStream使用:public class ReportExporter {    public void exportReport(String reportPath, List&amp;lt;String&amp;gt; data) throws IOException {        try (BufferedOutputStream bos = new BufferedOutputStream(                new FileOutputStream(reportPath))) {            for (String line : data) {                bos.write(line.getBytes());                bos.write(&#39;\n&#39;);            }            // BufferedOutputStream会在close时自动flush,也可手动flush            bos.flush();        }    }}DataOutputStream类型化写入​public class BinaryConfigWriter {    public void saveDatabaseConfig(String path) throws IOException {        try (DataOutputStream dos = new DataOutputStream(                new FileOutputStream(path))) {            dos.writeBoolean(true);                    // 写入布尔值            dos.writeInt(100);                         // 写入整数            dos.writeUTF(&quot;jdbc:mysql://localhost:3306/app&quot;); // 写入字符串        }    }}ObjectOutputStream对象序列化​public class SessionPersistence {    public void saveSession(UserSession session, String path) throws IOException {        try (ObjectOutputStream oos = new ObjectOutputStream(                new FileOutp...</content>
<url>https://zwany1.github.io/2025/12/26/IO%E5%9F%BA%E7%A1%80%E4%B8%8E%E6%B5%81%E6%93%8D%E4%BD%9C/</url>
</entry>
<entry>
<title>Best buddy</title>
<content>Best Friend Memoir这是我最好的朋友的故事。我们一起经历了许多难忘的时光，分享了无数的欢乐和泪水。    您的浏览器不支持视频播放。相识我们是在高中时认识的，……………………….共同的回忆  一起熬夜完成编程作业  周末一起去图书馆学习  假期一起旅行探索新的城市  遇到困难时互相支持和鼓励友谊的意义真正的友谊是经得起时间考验的。无论我们相隔多远，联系多频繁，那份深厚的友谊永远不会改变。  “朋友是生活中最美好的礼物之一。”感谢我的 Best buddy，让我的生活变得更加丰富多彩。</content>
<url>https://zwany1.github.io/2025/12/08/best-buddy/</url>
</entry>
</search>
